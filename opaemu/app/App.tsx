// App.tsx


/**
 * expo-router를 사용하면 App.tsx는 내비게이션 설정에 사용되지 않습니다.
 * 모든 라우팅과 레이아웃 설정은 'app/_layout.tsx' 파일에서 처리됩니다.
 *
 * 이 파일은 삭제하거나, 나중에 앱 전체에 적용할 Provider(상태 관리 등)를 
 * 설정하는 용도로 남겨둘 수 있습니다.
 */
export default function App() {
  // expo-router가 'app/_layout.tsx'를 앱의 진입점으로 사용하므로
  // 이 파일은 현재 특별한 역할을 하지 않습니다.
  return null;
}